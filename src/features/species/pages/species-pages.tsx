import{Droplets,Ruler,Search,Thermometer,Utensils,type LucideIcon}from'lucide-react';import{useState}from'react';import{Link,useParams}from'react-router-dom';import{SpeciesCard}from'@/components/content-cards';import{species}from'@/services/mock-data';
const cats=['All','Snakes','Lizards','Turtles','Amphibians','Invertebrates','Fish','Birds','Mammals'];export function SpeciesPage(){const[q,setQ]=useState(''),[cat,setCat]=useState('All');const list=species.filter(s=>(cat==='All'||s.category===cat)&&`${s.commonName} ${s.scientificName}`.toLowerCase().includes(q.toLowerCase()));return <div className="container page"><span className="kicker">Species encyclopedia</span><h1>Know the animal.<br/><em>Understand their world.</em></h1><p className="lede">Evidence-minded profiles for responsible, species-first care.</p><label className="filter"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search common or scientific name"/></label><div className="chips">{cats.map(x=><button className={cat===x?'active':''} onClick={()=>setCat(x)} key={x}>{x}</button>)}</div><div className="grid three">{list.map(x=><SpeciesCard item={x} key={x.slug}/>)}</div></div>}
export function SpeciesDetailPage(){const{slug}=useParams();const s=species.find(x=>x.slug===slug)||species[0];const careItems:Array<[LucideIcon,string,string]>=[[Ruler,'Adult size',s.adultSize],[Thermometer,'Temperature',s.temperature],[Droplets,'Humidity',s.humidity],[Utensils,'Lifespan',s.lifespan]];return <><section className="detail-hero"><img src={s.image} alt={s.commonName}/><div className="container"><Link to="/species">← Encyclopedia</Link><h1>{s.commonName}</h1><i>{s.scientificName}</i></div></section><div className="container detail"><article><span className="kicker">Species profile</span><h2>Thoughtful care begins with context.</h2><p>{s.habitat}. Provide secure hides, enrichment, clean water, and a stable environment that supports natural behavior.</p><h2>Feeding</h2><p>{s.feeding}. Track weight and body condition to guide adjustments.</p><h2>Morph information</h2><p>{s.morphs}. Choose health and responsible breeding over appearance alone.</p><h2>Gallery & related articles</h2><div className="mini-gallery"><img src={s.image} alt={s.commonName}/><img src={species[1].image} alt="Related species"/></div></article><aside className="care"><h3>Care essentials</h3>{careItems.map(([Icon,n,v])=><div key={n}><Icon/><span>{n}<b>{v}</b></span></div>)}<button className="btn">Save to favorites</button></aside></div></>}
