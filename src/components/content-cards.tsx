import{ArrowUp,Clock,MessageCircle}from'lucide-react';import{Link}from'react-router-dom';import{Badge}from'@/components/ui/badge';import type{Article,CommunityPost,Species}from'@/types/content';
export function SpeciesCard({item}:{item:Species}){return <Link className="card species-card" to={`/species/${item.slug}`}><div className="image"><img src={item.image} alt={item.commonName}/><Badge>{item.careLevel}</Badge></div><div className="body"><span>{item.category}</span><h3>{item.commonName}</h3><i>{item.scientificName}</i><p>{item.habitat}</p><b>View species profile →</b></div></Link>}
export function ArticleCard({item}:{item:Article}){return <article className="card article-card"><img src={item.image} alt=""/><div className="body"><span>{item.category}</span><h3>{item.title}</h3><p>{item.excerpt}</p><small><Clock/> {item.readTime}</small></div></article>}
export function PostCard({item}:{item:CommunityPost}){return <article className="card post"><div className="vote"><ArrowUp/><b>{item.votes}</b></div><div><Badge>{item.category}</Badge><h3>{item.title}</h3><small>@{item.author} · {item.createdAt} · {item.species}</small></div><small><MessageCircle/>{item.replies}</small></article>}
